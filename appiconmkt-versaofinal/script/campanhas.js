
export function inicializarCampanhas(){
    /*let todo = document.getElementById("todo-column");
    let draggedElement = null;

todo.addEventListener("dragover", function drag(event) {
  draggedElement = event.target;
});

function allowDrop(event) {
  event.preventDefault();
}

function drop(event, targetColumnType) {
  event.preventDefault();

  if (draggedElement) {
    const column = event.currentTarget;
    column.appendChild(draggedElement);

    // Remove todas as classes de status
    draggedElement.classList.remove("todo", "done");

    // Adiciona a nova com base na coluna
    draggedElement.classList.add(targetColumnType);

    draggedElement = null;
  }

}*/}